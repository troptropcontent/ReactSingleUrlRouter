{
  "name": "react-static-url-router",
  "version": "0.0.1",
  "description": "A router that works with query params to allow routing feature in one signe URL, useful when you create a React App that is meant to be implemented in several websites on wich you do not have control other DNS settings. With this the page can be refetch and reapear as it was left.",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/troptropcontent/ReactSingleUrlRouter"
  },
  "scripts": {
    "test": "jest --config jestconfig.json",
    "build": "tsc",
    "lint": "eslint  --ignore-path .gitignore --ext .js,.ts .",
    "format": "prettier --ignore-path .gitignore --write \"**/*.+(js|ts|json)\""
  },
  "author": "https://github.com/troptropcontent",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/react": "^18.2.38",
    "@typescript-eslint/eslint-plugin": "^6.12.0",
    "@typescript-eslint/parser": "^6.12.0",
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2"
  },
  "peerDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
